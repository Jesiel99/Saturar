extends Spatial

#var timer : = 0.0
#var audio_file = "res://Assets/Audio/10 comerciais que marcaram os Anos 2000.mp3"
#
#func _process(delta):
#	timer += delta
#	if int(timer) % 300 == 0:
#		if File.new().file_exists(audio_file):
#			var sfx = load(audio_file) 
#			sfx.set_loop(true)
#			var speech_player = AudioStreamPlayer2D.new()
#			speech_player.stream = sfx
#			speech_player.play()
#			add_child(speech_player)

